কখন ব্যবহার করব Dutch National Flag Algorithm?
যখন তোমার কাছে একটা array থাকবে যেটায় শুধু ৩ ধরনের এলিমেন্ট আছে (যেমন: 0, 1, 2) এবং তোমাকে ওই array-টা এই ৩ ধরনের এলিমেন্টকে একটা নির্দিষ্ট ক্রমে সাজাতে হবে — তখন এই অ্যালগরিদম খুব কাজে লাগে।

কীভাবে কাজ করে:
তিনটা pointer বা index রাখবে:

low → ০ গুলোর জন্য পরের জায়গা

mid → বর্তমানে যে element দেখছ

high → ২ গুলোর জন্য পরের জায়গা

একবার array-র মধ্য দিয়ে mid pointer চলে যাবে, আর প্রতিবার:

যদি nums[mid] == 0 হয় → nums[low] এর সাথে swap করো, low++, mid++

যদি nums[mid] == 1 হয় → mid++ (কিছুই swap করতে হবে না)

যদি nums[mid] == 2 হয় → nums[mid] এবং nums[high] swap করো, high-- (কিন্তু mid একই থাকবে কারণ নতুন value দেখে নিতে হবে)



খুব ভাল প্রশ্ন! Dutch National Flag Algorithm-এ আমরা যখন nums[mid] == 0 পাই, তখন low এবং mid দুটো pointer-ই কেন ++ করি—এইটা এখন ব্যাখ্যা করছি:
if (nums[mid] == 0) {
    swap(nums[low], nums[mid]);
    low++;
    mid++;
}

🔎 কেন swap করি?
আমাদের লক্ষ্য:

সব 0 বামদিকে যাবে,

সব 2 ডানদিকে যাবে,

সব 1 মাঝখানে থাকবে।

তাই 0 পেলে, সেটা low index-এ পাঠিয়ে দেই (swap করে)।

❓ কেন low++ করি?
low pointer এমন জায়গা দেখায়, যেখান থেকে এখনো কোনো 0 রাখা হয়নি। আমরা যদি একটাও 0 পাই, তাহলে সেটা low-এর জায়গায় বসিয়ে low++ করে দিই যেন পরবর্তী 0 এর জন্য জায়গা তৈরি হয়।

❓ তাহলে mid++ কেন?
mid হচ্ছে আমাদের current index যেটা চেক করছি। যেহেতু swap() করার পর আমরা জানি nums[mid] এ এখন এমন একটা মান আছে যেটা আগেই দেখা হয়ে গেছে (কারণ এটা low থেকে এসেছে), তাই এখন আমরা mid++ করি যাতে পরবর্তী element-এ move করতে পারি।

nums = [0, 1, 2]
        ↑
       mid, low
nums[mid] == 0, তাই:

swap(nums[low], nums[mid]) → কিছুই পরিবর্তন হল না (0 আর 0 swap)

low++ → 1, mid++ → 1

এখন আমরা পরবর্তী index (1) check করতে পারব।

⚠️ যদি mid++ না করতাম?
তাহলে সেই পুরানো low থেকে swap করা মানটা আবার check করতে হত, যেটা একবার দেখা হয়ে গেছে—redundant হতো।



 **কেন mid++ করিনি else এ?
কারণ:

আমরা যখন 2 পাই, তখন সেটা high pointer-এর সাথে swap করি।

কিন্তু, high-এর জায়গায় যেটা এসেছে, সেটা এখনো check করা হয়নি!

তাই, আমরা mid-কে আগাবো না, যেন সেই নতুন nums[mid] আবার check করতে পারি।
nums = [2, 0, 1]
         ↑     ↑
        mid   high

nums[mid] == 2 → swap(nums[mid], nums[high])
→ এখন nums = [1, 0, 2], mid = 0, high = 1

❗ এখন যদি mid++ করে দিতাম, তাহলে nums[0] == 1 check না করেই skip হয়ে যেত।

তাই, mid কে আগায় না—আগামী iteration-এ ওই নতুন nums[mid] আবার check করি।



| Case                           | Example                  |
| ------------------------------ | ------------------------ |
| 3 fixed values only            | `[0, 1, 2, 1, 0, 2]`     |
| Grouping                       | RED, WHITE, BLUE balls   |
| Partitioning based on category | Negative, Zero, Positive |
